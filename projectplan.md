# Project Configuration

## Primary Color

The primary color for this project is `#e68200`. This color is defined in `tailwind.config.ts` as `primary`.

All buttons should use this primary color.

## Public Routes

All public routes are defined in `src/utils/public_routes.ts`. Whenever a new public route is created, remember to add it to this file to ensure consistency across the application.

